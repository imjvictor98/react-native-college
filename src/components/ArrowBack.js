import { Icon } from 'native-base';
import styled from 'styled-components/native';

const ArrowBack = styled(Icon).attrs({
  name: 'ios-arrow-back',
})`
  color: #3f444d;
`;

export default ArrowBack;
